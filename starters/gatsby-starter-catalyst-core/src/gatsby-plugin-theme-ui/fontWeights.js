import merge from "deepmerge"
import { BaseThemeFontWeights } from "gatsby-theme-catalyst-core"

export default merge(BaseThemeFontWeights, {
  // text: "400",
  // heading: "700",
  // bold: "700",
})
